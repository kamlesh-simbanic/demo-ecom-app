import "server-only";
export const serverSideSection = () => {
  console.log(`use multiple library, 
    use environment variables,
    interact with a database,
    process confidential information
  `);

  return "Server result";
};
