import { options, services, summary } from "./data";
// import Summary from "./Summary";
// import Table from "./Table";

type Props = {
  params: {
    id: string;
  };
};

const Services = async ({ params: { id } }: Props) => {
  return <>Services</>;
};

export default Services;
